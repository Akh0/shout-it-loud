import React from 'react'
import styled from '@emotion/styled'

const StyledTitle = styled.h1`
  margin: 2vh;
  text-align: center;
  font-size: 6vh;
  line-height: 8vh;
  height: 8vh;
`

const Title = () => <StyledTitle>Shout it loud!</StyledTitle>

export default Title
